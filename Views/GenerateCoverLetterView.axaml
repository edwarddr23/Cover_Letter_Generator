<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:CoverLetterGenerator.ViewModels"
             xmlns:models="using:CoverLetterGenerator.Models"
             xmlns:conv="using:CoverLetterGenerator.Converters"
             x:Class="CoverLetterGenerator.Views.GenerateCoverLetterView"
             x:DataType="vm:GenerateCoverLetterViewModel">

    <Design.DataContext>
        <vm:GenerateCoverLetterViewModel/>
    </Design.DataContext>

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="10">
        <TextBlock Text="{Binding PageTitle}" FontSize="20"/>

        <!-- Templates Selection -->
        <TextBlock>Templates:</TextBlock>
        <ItemsControl ItemsSource="{Binding TemplateNames}">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="models:ChoiceItem">
                    <RadioButton Content="{Binding Name}"
                                IsChecked="{Binding IsSelected, Mode=TwoWay}"
                                GroupName="TemplateGroup" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Fix AVLN2000: Ensure proper binding -->
        <TextBlock Text="{Binding SelectedTemplate, StringFormat='Selected: {0}'}" Margin="0,10,0,0"/>
        
        <!-- Job Source Input -->
        <TextBlock>Job Source:</TextBlock>
        <TextBox Width="200" Text="{Binding JobSource, Mode=TwoWay}" Watermark="Enter job source"/>

        <!-- Generate Button -->
        <Button Command="{Binding GenerateCoverLetterCommand}" Content="Generate Cover Letter" Margin="0,10,0,0"/>
    </StackPanel>
    
</UserControl>